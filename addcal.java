package Calculator;

public class addcal extends Basic {
    public double pow(int n1, int n2) {
        return Math.pow(n1, n2);
    }

    public int min(int n1, int n2) {
        return Math.min(n1, n2);
    }

    public int max(int n1, int n2) {
        return Math.max(n1, n2);
    }
}
